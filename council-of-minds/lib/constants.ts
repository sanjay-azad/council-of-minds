/** Speakers per batch before auto-pause */
export const SPEAKERS_PER_BATCH = 3;

/** Max debate messages (excluding verdict) before forcing verdict */
export const MAX_DEBATE_MESSAGES = 30;

/** Max rounds (each round = all 6 personas speak once) */
export const MAX_ROUNDS = 5;

/** Max output tokens per debater response */
export const DEBATER_MAX_TOKENS = 250;

/** Max output tokens for The Judge verdict (6 structured sections) */
export const JUDGE_MAX_TOKENS = 1000;
